---
const { variant } = Astro.props;

const base = "rounded-4xl px-4 py-2 button cursor-pointer";

const variants = {
  header: "btn-header text-dark",
  light: "btn-light text-dark",
  dark: "btn-dark text-light",
};
---

<button class={`${base} ${variants[variant]}`}>
  <slot />
</button>
